aiofiles>=23.2.1
###############################################
# asrpro CORE requirements (Windows friendly) #
###############################################
# Pin to versions with prebuilt wheels to avoid local C/C++ builds.
# Parakeet (NeMo) is OPTIONAL now – see requirements-nemo.txt.

# GUI / Server
PySide6==6.9.2
fastapi==0.116.1
uvicorn==0.35.0
python-multipart==0.0.20
aiofiles==24.1.0
psutil==6.1.0            # Process management for single instance

# ML / Whisper stack
torch==2.8.0            # Keep in sync with torchaudio
torchaudio==2.8.0
faster-whisper==1.2.0    # ONNX / CTranslate2 based Whisper
ctranslate2==4.6.0
transformers==4.53.3
sentencepiece==0.2.1

# Audio + DSP
sounddevice==0.5.2
soundfile==0.13.1
pydub==0.25.1
librosa==0.11.0
numpy==1.26.4            # Compatible with torch 2.8.0
scipy==1.16.1
ffmpeg-python==0.2.0
soxr==1.0.0              # pulled indirectly but pin for reproducibility

# Utility
pynput==1.8.1
omegaconf==2.3.0

# NOTE:
# 1. For Parakeet (NeMo) models, ALSO install requirements-nemo.txt
# 2. Ensure FFmpeg is installed (https://www.gyan.dev/ffmpeg/builds/ or winget install ffmpeg)
# 3. CUDA users: install matching Torch CUDA build BEFORE this file if needed.

